import { FilterbyroleInterceptor } from './filterbyrole.interceptor';

describe('FilterbyroleInterceptor', () => {
  it('should be defined', () => {
    expect(new FilterbyroleInterceptor()).toBeDefined();
  });
});
